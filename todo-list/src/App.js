import Home from './Home';

// Additional notes in CreateTodos.js

// Can strikeout and delete tasks. 
    // add checkbox which strikes out task. Can toggle strikeout.
    // Can only delete items if stuff is stiked out. if (id is xxx && isComplete)


    // See if objects can be saved to JSON later on, then they can be moved to different computers.

function App() {
    return (
        <>         
          <Home />
        </>
    );
}

export default App;
